This is a [Next.js](https://nextjs.org) project 


CSS filter

1. uses CSS varibles to filter an image
2. uses Useref to transfer parent element to child component and vice versa. Component(HTML element) as a prop from top to bottom; and supported ref(in react 19) from bottom to up
3. uses flowbite (vanilla flowbite) , no react Component. I found that react-flowbite is terrible. it's better to use flowbite or daisyUI as pure JS/CSS libs, because the developer needs to know all tailwind classes used internally.
